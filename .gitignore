# ── Large simulation inputs/outputs (produced by Component 1, not versioned) ──
# The APSIM grid results are big (~30 MB) and machine-produced. Only the small
# aggregated app/data/yield-surface.csv is committed.
simulation/data/outputs/simulated-scenarios-df.rds
simulation/data/outputs/checkpoints/
simulation/data/outputs/apsim-work/
simulation/data/outputs/*.log
simulation/data/raw/weather/
simulation/data/raw/soil/
simulation/intermediate-data/

# ── R session / IDE cruft ────────────────────────────────────────────────
.Rhistory
.Rapp.history
.RData
.Ruserdata
.Rproj.user/
*.Rproj.user

# ── shinylive build output (regenerated by CI) ───────────────────────────
/_site/
app/_site/
docs/app/
site/

# ── stray R plot device output ───────────────────────────────────────────
Rplots.pdf
**/Rplots.pdf

# ── OS ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# In-season pipeline local cache (weather + APSIM outputs are regenerated weekly)
current_season/data/weather/
current_season/data/outputs/
